# Copy to .env locally. Never commit real secrets.
# IMPORTANT: This file must NOT contain real credentials.
# If you add new required environment variables, add them here with empty or mock values.

# Example placeholders only:
# API_BASE_URL=https://api.example.com
# ANALYTICS_ID=mock-analytics-id
# CMS_URL=https://cms.example.com
# OPENAI_API_KEY=sk-mock...
# AWS_ACCESS_KEY_ID=AKIA... (do not add real key)
# AWS_SECRET_ACCESS_KEY=... (do not add real key)
