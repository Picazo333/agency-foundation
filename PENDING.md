# Pending

Deferred intentionally until after brand naming + Brand Direction V0 unless a security reason makes earlier action preferable:

- [ ] Change repository visibility from **public** to **private**.
- [ ] Import/sync the complete raw ZIP research packs and remaining binary/image assets into the repo or approved external storage with manifests/provenance.
- [ ] Review whether large binary assets should use Git LFS or external storage before bulk import.
- [ ] Review and delete/retire unused helper branches created during setup (`ignore`, `ops/worktree-guide`, `ops/worktree-guide-2`, `ops/worktree-guide-3`, and the superseded `tech/agent-foundation`) after confirming none contains unique work worth preserving.

Do not block the current Brand workstream on these items, but do not assign new work to superseded/helper branches.
