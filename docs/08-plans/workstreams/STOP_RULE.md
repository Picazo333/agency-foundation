# Stop Rule

If a worker reaches a decision owned by another workstream, it should record the dependency/open question and stop that branch of work rather than silently deciding outside its authority.
