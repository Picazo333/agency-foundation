# Parallel-Work Principles

- Parallelize only work with independent inputs.
- Do not duplicate authority across agents.
- Every workstream declares forbidden scope.
- Every workstream produces versioned artifacts and a handoff.
- Convergence happens at explicit gates, not by informal chat consensus.
- If outputs conflict, reopen the relevant decision instead of silently blending them.
